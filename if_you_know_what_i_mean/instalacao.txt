node ultima versão:
curl -sL https://deb.nodesource.com/setup | sudo bash -
sudo apt-get update
sudo apt-get install nodejs

atualizando npm(para 3.3.12:
sudo npm install -g npm

atualizando node(para o 5.0.0):
sudo npm cacheclean -f
sudo npm install -g n

instalando o mongo:

instalando bower:
npm install -g bower

Inicializando mean CLI:
npm install -g mean-cli
mean init <myApp>
cd <myApp> && npm install